spring.application.name=url-shortner

# Base of the generated short URLs; the short code is appended to it.
# Override per environment, e.g. APP_BASE_URL=https://sho.rt/
app.base-url=https://openshortner.com/
